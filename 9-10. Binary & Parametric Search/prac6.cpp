#include <bits/stdc++.h>

using namespace std;

int arr[104];
int length[104];

int main(void) {

    int N, M, L;
    cin >> N >> M >> L;

    for(int i=0; i<N; i++) {
        cin >> arr[i];
    }

    sort(arr, arr+N);


    return 0;   
}