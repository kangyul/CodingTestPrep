#include <bits/stdc++.h>

using namespace std;

int arr[100004];

int main(void) {

    ios::sync_with_stdio(0); cin.tie(0);

    int N;
    cin >> N;

    for(int i=0; i<N; i++) {
        int x;
        cin >> x;
        arr[i] = x;
    }

    sort(arr, arr+N);

    int s = 0;
    int e = N-1;
    
    while(s<=e) {
    
        

    }

    return 0;
}

